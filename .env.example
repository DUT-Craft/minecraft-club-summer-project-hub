# ESA 静态部署必须使用可从浏览器访问的后端地址
NUXT_PUBLIC_API_BASE=https://api.project.doh.ink/api

# 是否启用 nekoFrontend 登录拦截；false 时公开项目站仍可直接访问
NUXT_PUBLIC_AUTH_CHECK_ENABLED=false

# 本地 JSON 数据目录，默认就是 ./data
LOCAL_DATA_DIR=./data
